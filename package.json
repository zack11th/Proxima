{
  "name": "proxima",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "game": "concurrently \"(npm start --prefix client)\" \"(npm start --prefix server)\""
  },
  "author": "owl on globe",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
